"""Legacy FrameExecutor name retained during runtime migration."""

from stanalyzer.runtime import RuntimeExecutor


class FrameExecutor(RuntimeExecutor):
    """Deprecated name retained while analysis modules migrate."""
